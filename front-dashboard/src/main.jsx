import { StrictMode } from 'react'
import Rotas from './rotas/header/Rotas'
import React from 'react'
import ReactDOM from 'react-dom/client';


ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
   
      <Rotas/>
   
  </React.StrictMode>,
)
